export default function FlashcardsTab({ conversationId }: { conversationId: string }) {
  return (
    <div className="p-6 text-gray-400">
      <p>Flashcards tab for conversation {conversationId} — coming in a later phase.</p>
    </div>
  );
}