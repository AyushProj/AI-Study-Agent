export default function ChatTab({ conversationId }: { conversationId: string }) {
  return (
    <div className="p-6 text-gray-400">
      <p>Chat tab for conversation {conversationId} — chat UI coming soon.</p>
    </div>
  );
}