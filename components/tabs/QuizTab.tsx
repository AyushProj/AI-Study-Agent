export default function QuizTab({ conversationId }: { conversationId: string }) {
  return (
    <div className="p-6 text-gray-400">
      <p>Quiz tab for conversation {conversationId} — coming in a later phase.</p>
    </div>
  );
}